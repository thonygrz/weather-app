[build]
  command = "npm run build"
  publish = ".output/public"      # Directorio con los activos estáticos

[functions]
  directory = ".output/server"    # Directorio con las funciones SSR (Nitro)
